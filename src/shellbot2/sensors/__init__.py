"""Packaged sensor implementations for ShellBot2.

Each module exports ``SENSOR_SPECS``. Plugin contracts and the scheduler live
in ``shellbot2.sensorframework``. Custom sensors can also be added as ``*.py``
files under ``<datadir>/sensors/``.
"""
